# proyecto Git/Github

Paso a paso para hacer seguimiento (control de versiones) a un repositorio Git:

0. Crer (Git init) o clonar (git clone)
1. Crear archivo .gitignore
2. Usar git add para hacer seguimineto (rastreo archivos)